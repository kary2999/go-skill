# Team Standards 使用指导

Go 微服务团队编码规范 · Claude Code + Cursor 双平台一键安装 App。

---

## 一、快速上手（3 分钟）

### 1.1 本地使用（自己电脑）

```bash
# 直接双击
Team Standards.app
```

或从工作目录启动：
```bash
open "~/team-standards/Team Standards.app"
```

App 启动后会出现一个带 🦕 Logo 的原生窗口，左侧 3 个 Tab：
- 📚 **规范模块** — 浏览所有规则
- 🦕 **SM · Skill 管理** — 添加你自己的规则
- ⚡ **安装** — 一键安装 + 打包分发

### 1.2 首次安装规范

1. 点左侧 **⚡ 安装**
2. 选 **全局安装** + **两个都装**
3. 点 **⚡ 立即安装（覆盖已有）**
4. **重启 Claude Code / Cursor** 即生效

安装位置：
- Claude：`~/.claude/skills/go-team-standards/`
- Cursor：`~/.cursor/rules/`

---

## 二、在 Cursor / Claude Code 中使用

### 2.1 自动触发（不用任何操作）

规则按 `.mdc` 文件的 `globs` / `alwaysApply` 自动生效：

| 规则 | 何时触发 |
|---|---|
| 铁律 / 命名 / 日志 / 安全红线 / demo 路由 | **永远生效** |
| Go 编码 / 错误码 / common-lib 优先 | 改 `**/*.go` 时 |
| API 设计 | 改 `.proto` 或 `handler / service / server/**` |
| PostgreSQL 设计 | 改 `migrations/**` 或 `*.sql` |
| 单元测试 | 改 `**/*_test.go` |
| Commit / CI | 手动 `@` 调用 |

### 2.2 模糊自然语言触发 Demo

在聊天框用大白话描述需求，AI 会自动找对应模板：

| 你说 | AI 会参考 |
|---|---|
| "帮我写个 kafka 生产者" | `demos/kafka-producer.go` |
| "给个 redis 幂等示例" | `demos/redis-idempotency.go` |
| "pg 建表模板" | `demos/pg-migration.sql` |
| "新建一个 kratos 服务" | `demos/kratos-service-min.go` |
| "错误码怎么用 / errno" | `demos/errno-xerror.go` |
| "打日志 / trace_id" | `demos/slog-trace.go` |
| "写单测 / 表驱动" | `demos/table-driven-test.go` |
| "gorm crud / 游标分页" | `demos/pg-gorm-repo.go` |
| "wire 依赖注入" | `demos/wire-providerset.go` |

**不需要精确关键词**，意图对即可。

### 2.3 违规提示三段式

AI 检测到违规时**必须**按以下格式回复（已写入铁律）：

1. **哪条规则** — 引用铁律编号或规则名
2. **为什么** — 说明违反的后果（稳定性 / 精度 / 合规 / 性能）
3. **怎么改** — 给出修正代码

举例：你写 `errors.New("order not found")` + `SELECT *`，AI 会说：

> 发现违反**铁律 2**（错误码体系）和**铁律 9**（禁止 SELECT *）。
>
> **为什么**：
> - 裸字符串错误让 Loki 无法按 `error_code` 聚合，前端无法国际化
> - `SELECT *` 在加新字段后拉多余数据、破坏隐式契约
>
> **改为**：
> ```go
> db.Select("id, user_id, status").Where("id = ?", id).First(&o)
> return nil, xerror.New(errno.OrderNotFound)
> ```

---

## 三、添加自己的 Skill（SM 功能）

### 3.1 步骤

1. 打开 App → 点 **🦕 SM · Skill 管理**
2. 右侧表单填写：
   - **规则名**：例如 "所有 Go 代码必须带解释性注释"
   - **简介**：英文短句，进入 `.mdc` description 供 AI 识别
   - **为什么**：违反的后果（AI 会用这段解释违规）
   - **规则内容**：具体要求，用 `-` 列出子项
   - **作用范围**：按文件匹配 / 永远生效
3. 点 **💾 保存并同步**

保存后立即同步到：
- `~/.cursor/rules/custom-<slug>.mdc`
- `~/.claude/skills/go-team-standards/references/custom-<slug>.md`

### 3.2 预设模板（一键套用）

左下有 3 个现成模板点 **「套用」** 按钮填入表单：
- Go 代码必带解释性注释
- 所有对外写接口必须幂等
- 禁止硬编码 sleep 时长

### 3.3 编辑 / 删除

列表里每条规则都有 **编辑** / **删除** 按钮。删除会同步从 `~/.cursor/rules/` 和 Claude references 移除。

---

## 四、打包分发给同事

### 4.1 生成分发包

App → **⚡ 安装** Tab → 底部 **📦 打包分发给他人** → 点 **⬇️ 生成分发包**

打包完成后会显示：
- 文件名（带版本号 + 时间戳）
- 绝对路径
- **🔍 在 Finder 中显示** 按钮

所有 zip 归档在 `~/skills-version/`，不会覆盖历史。

### 4.2 zip 包内容

```
TeamStandards-v1.0.0-YYYYMMDD-HHMM.zip
├── Team Standards.app/                  ← Universal Binary，M1-M4 + Intel 通吃
├── 使用说明.md                          ← 中文安装指南
└── 首次打开失败-双击修复.command           ← 解锁救援脚本
```

### 4.3 对方怎么装

对方收到 zip 后：
1. 双击解压
2. 把 `Team Standards.app` 拖到 `/Applications`（或桌面）
3. **右键 → 打开 → 打开**（首次一次，之后双击即可）
4. App 窗口弹出 → 点 **⚡ 立即安装**
5. 重启 Claude / Cursor

**全程无需终端、无命令行、无 Go 环境。**

### 4.4 万一报"已损坏"

macOS 对未从 App Store 下载的应用默认拦截，显示"已损坏"是误报。解锁四选一：

| 方法 | 操作 |
|---|---|
| A | 双击 zip 里的 `首次打开失败-双击修复.command`，自动修复 |
| B | `.app` 上 **右键 → 打开 → 打开** |
| C | 终端：`xattr -cr /Applications/Team\ Standards.app` |
| D | 系统设置 → 隐私与安全性 → 点「仍要打开」 |

---

## 五、规范详情查看

### 5.1 App 内查看

点 **📚 规范模块** → 点任意卡片 → 弹出 Modal 显示完整 Markdown 原文。

### 5.2 直接看文件

- 项目内源文件：`~/team-standards/standards/*.md`
- 已安装的 Claude references：`~/.claude/skills/go-team-standards/references/`
- Cursor rules：`~/.cursor/rules/*.mdc`

---

## 六、规范一览

### 6.1 永远生效的铁律（12 条）

1. 禁止硬编码密钥
2. 业务错误走 `xerror.New(errno.XxxCode)`
3. error 必检（禁 `_` 丢弃）
4. 业务禁 `panic`
5. goroutine 必须有退出机制
6. 金额用 `decimal.Decimal`，禁 `float64`
7. 时间用 `TIMESTAMPTZ(6)` UTC
8. 日志只用 `slog`，必带 `trace_id`
9. 禁止 `SELECT *` 和 N+1
10. 禁止数据库外键
11. 禁止注释掉的死代码
12. 敏感数据禁入 prompt / 日志

### 6.2 技术栈前提

- **Kratos v2** + **Wire DI** + **Buf proto**
- **PostgreSQL** + **Kafka** + **Redis**
- **`mask-go-common-lib`** 团队公共库

**禁止绕过 common-lib 直连底层库**（裸 sarama / go-redis / otel）。

替换表见 `cursor/rules/10-common-lib.mdc`。

---

## 七、版本管理

- 当前版本：`VERSION` 文件（目前 1.0.0）
- 变更记录：`CHANGELOG.md`
- 历史分发包：`~/skills-version/`

改规范的流程：
1. 改 `standards/*.md`（SSOT）
2. 同步修改 `cursor/rules/*.mdc` 和 demos/（如果影响要点）
3. Bump `VERSION`，写 `CHANGELOG.md`
4. `./build-mac-app.sh` 重新打包
5. App 里点生成分发包 → 分发新 zip

---

## 八、排错

### 8.1 App 双击没反应

- 检查进程：`pgrep -l team-standards-installer`
- 手动启动看日志：`"./Team Standards.app/Contents/MacOS/team-standards-installer"`

### 8.2 装了规则但 Cursor 没反应

1. **彻底重启 Cursor**（Cmd+Q 后再打开，不是 reload window）
2. Cursor 需要 **0.42+** 才支持 `.cursor/rules/*.mdc`
3. Cursor 设置 → Rules → 应该看到 `~/.cursor/rules/` 下的 `.mdc` 列表

### 8.3 装了规则但 Claude Code 没反应

1. 重启 Claude Code
2. `ls ~/.claude/skills/go-team-standards/` 确认文件在
3. `SKILL.md` 第一行的 `name: go-team-standards` 必须存在

### 8.4 自定义 Skill 保存后没同步

- 打开 App 时提示"Claude / Cursor 尚未安装" → 先去 **⚡ 安装** Tab 装一次
- 之后添加自定义 Skill 会自动合并进两个目录

### 8.5 重新打包 App

```bash
cd ~/team-standards
./build-mac-app.sh
```

会生成 Universal Binary（arm64 + x86_64）+ ad-hoc 签名 + 🦕 图标。

---

## 九、常见问题

**Q: 我没装 Claude / Cursor 也能装规则吗？**  
A: 能。目录不存在时会自动创建。以后装 Claude / Cursor 就能直接加载。

**Q: 能只装 Claude，不装 Cursor 吗？**  
A: 能。安装 Tab 里选「仅 Claude Skill」即可。

**Q: 规则会影响我所有项目吗？**  
A: 全局安装会影响所有 Go 项目。想按项目隔离，选「项目安装」指定目录即可，只装到 `<项目>/.claude/` 和 `<项目>/.cursor/`。

**Q: 团队规范更新了怎么同步？**  
A: 拉最新版本 → `./build-mac-app.sh` → App 里点「立即安装（覆盖已有）」即可。旧规则会被完全覆盖，自定义 Skill 保留。

**Q: 自定义 Skill 存在哪？会被覆盖吗？**  
A: 存在 `~/.team-standards/custom-rules.json`。重装团队规范时不会动它们 —— 覆盖安装只清理 `NN-*.mdc` 格式的团队规则，`custom-*.mdc` 永远保留。

**Q: zip 包怎么发给同事？**  
A: 任何方式都行 —— 企业 IM、邮件、AirDrop、U 盘。macOS 会给下载/转发的 app 打上隔离标记，对方用上面第四节的方法解锁一次即可。

---

## 十、文件地图

```
~/skills/
├── team-standards/              ← 项目根
│   ├── VERSION                  ← 版本号
│   ├── CHANGELOG.md             ← 变更日志
│   ├── README.md                ← 技术文档
│   ├── 使用指导.md               ← 本文件
│   │
│   ├── standards/               ← 规范原文（SSOT）
│   ├── demos/                   ← 代码模板
│   ├── claude/go-team-standards/← Claude Skill 产物
│   ├── cursor/                  ← Cursor rules 产物
│   ├── assets/.golangci.yml     ← lint 配置
│   │
│   ├── main.go + catalog.go + install.go + custom.go + zipgen.go  ← App 源码
│   ├── web/                     ← App 前端
│   ├── go.mod / go.sum
│   │
│   ├── team-standards-installer ← Universal Binary（编译产物）
│   ├── Team Standards.app       ← macOS .app bundle
│   └── build-mac-app.sh         ← 打包脚本
│
└── version/                     ← 历史分发包归档
    └── TeamStandards-v*.zip
```
