# 📤 提测报告（给测试同学）

> 生成时间：{YYYY-MM-DD HH:mm}  ·  生成工具：OrangeCat v{VERSION}
> 分支：`{branch}` → `{target_branch}`  ·  目标环境：{env}
> **提测人**（取自 `git log -1 --format='%an <%ae>'`）：{git_author_name} &lt;{git_author_email}&gt;

---

## 📖 1. 本次业务改动（**测试同学先看这段**）

> 从 commit messages + 代码注释（godoc / `//` 业务注释）深度提炼，给 QA 一个**业务维度**的全貌。
> 不是技术改动清单，是**做了什么业务 + 为什么**。

### 🧠 开发方的理解（开发一段话写，QA 对照需求看是否一致）

> **提测方自述**（由开发按自验 STEP 1 填写，然后 AI 附在此）：
>
> {开发写的一段话：这次改了什么、影响了哪个业务流程、在什么环节生效}

👉 **QA 同学请核对**：这段理解与需求单里的描述是否一致？如有出入，**请提测人（{git_author_name}）先对齐需求再提**。

---

### 🎯 做了什么（一句话总结）

{一句话说清本次提测的核心业务变化，例：「订单创建流程新增优惠券抵扣能力，支持满减和折扣两种券」}

### 📝 具体改动（按业务功能分组）

**功能 1：{功能名，例：优惠券抵扣}**
- 做了什么：{用户视角描述，例：下单时若选了优惠券，订单金额自动扣减}
- 涉及接口：`/api/v1/{path}`（详见 §3 接口变更清单）
- 涉及业务模块：`{模块/类名，例：OrderUsecase.Create + CouponValidator}`
- 证据 commit：`{hash1}` {commit title}；`{hash2}` {commit title}
- 关键代码注释摘要：
  > {引用代码里的 godoc 或中文业务注释原文，带文件路径，例：`internal/biz/order.go:45`："校验优惠券有效性，已过期或已使用的直接拒绝"}

**功能 2：……**

### 🤔 为什么改（业务动机）

{从 commit body / PR 描述 / JIRA 需求单 提炼。如果都没有，必须如实写"未从 commit/注释找到明确动机，需开发补充"，不许编}

### 🧭 测试重点建议

- 新增业务路径（必测）：{列出主流程}
- 边界/异常场景（建议覆盖）：{例：优惠券过期、用户无此权限、并发下单同一张券}
- 不会影响的部分（可不测）：{明确范围，给 QA 减负}

> ⚠️ 本段由 AI 从 `git log origin/master..HEAD --format='%H%n%s%n%n%b'` + `git diff` 影响文件的 godoc/注释 + 需求单链接 提炼。若你看了觉得不准确，请联系提测人（{git_author_name}）修正 commit message 或补充 PR 描述后重新生成。

---

## 📋 2. 本次提测概览

| 项目 | 内容 |
|---|---|
| 需求单 | {PRD 链接} |
| 任务单 | {JIRA 单号，多个用 / 分隔} |
| 分支 | `{branch}` → `{target}` |
| 预计回归范围 | {列出受影响的主流程} |
| 提测类型 | ☐ 新功能  ☐ Bug 修复  ☐ 数据迁移  ☐ 性能优化  ☐ 重构 |

---

## 🔌 3. 接口变更清单

> 测试同学重点看这段。每行「下游影响」必须写具体名字；如无下游，写验证方式（如：grep 无调用点）。

| 接口 | 方法 | 变更类型 | 入参变化 | 出参变化 | 下游影响 | 兼容性 |
|---|---|---|---|---|---|---|
| `/api/v1/xxx` | POST | 新增/修改/废弃 | {字段变化} | {字段变化} | {调用方 1}；{调用方 2} | ✅/⚠️/❌ |

> 若本次无接口变更：「本次提测未涉及对外接口变更」+ 依据说明。

---

## 🗄️ 4. SQL 数据影响（简版，上线顺序见开发版）

### 4.1 新增表

```sql
-- {migration 文件路径}
CREATE TABLE xxx (...)
```

### 4.2 修改表结构

```sql
-- {migration 文件路径}
ALTER TABLE xxx ADD COLUMN yyy ...;
```

> 测试同学只需知道数据是否会变、新字段是否影响测试环境数据；**详细的执行方式、回滚步骤见「开发版」**。

若本次无 SQL 变更：「本次提测未涉及 SQL 变更」。

---

## ✅ 5. 开发自测报告

> 与开发版同步。开发同学必须填，测试同学据此判断是否可接入测试。

- [ ] 已跑单元测试，通过率 **___ / ___**
- [ ] 已本地跑通改动接口的 happy path，链路：___
- [ ] 已覆盖失败/边界用例，列出：
    - ___ → 期望：___
- [ ] 已在 dev/test 环境自测（环境：___，时间：___）
- [ ] 既有相关用例回归通过（模块：___）
- [ ] 自测发现的问题及处理：
    - Bug1：___ → 已修复/已跟进（JIRA: ___）

---

> 📌 以上是提测给 QA 的全部必读内容。如需查看**脚本影响 / SQL 上线顺序 / 建议测试关注点 / 真实性校验**等开发侧信息，请查阅同目录下的 `提测报告_<branch>_<时间>_开发版.md`。
