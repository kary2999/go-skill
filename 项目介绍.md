---
title: "Team Standards · 项目介绍"
version: "1.7.28"
audience: "团队同事 / 评审 / 新成员"
last_modified: "2026-05-06"
---

# Team Standards

> **一个 macOS App，把团队规范装进 AI。让 AI 按团队规范干活，不再让规范烂在 Wiki。**

---

## 一句话总结

打开 App → 点"立即安装" → Claude Code 和 Cursor 立刻按你团队的 14 条铁律 / 命名规范 / 提测流程 / 个人偏好干活。**零命令行，零配置文件**，DMG 双击装好。

---

## 解决什么问题

### 痛点 1 · 规范烂在 Wiki

- 文档写了 50 页，新人入职抄三天，老员工凭"上次记得是这样"
- Code Review 反复扯皮：`float64` 金额、`user_id` 不叫 `uid`、`gmt_create` 又冒出来
- 同样的违规改不完，因为**规范不会自己跑到代码里**

### 痛点 2 · AI 不认识团队风格

- ChatGPT / Claude / Cursor 默认用的是开源世界平均做法
- 你团队的 errno 包、xerror、common-lib、命名约定，AI 不知道
- 写出来的代码每次都要改成"自己人能看的样子"

### 痛点 3 · 换电脑换账号要重来

- AI 客户端不记得你的偏好（Kratos 还是 Gin？slog 还是 zap？）
- 每个新项目重讲一遍"我喜欢 ..."

---

## 怎么解决

### 把规范变成 AI 主动加载的 Skill

**4 个 Claude Skill**（装到 `~/.claude/skills/`）：

| Skill | 触发场景 | 干啥 |
|---|---|---|
| 🌟 **go-team-standards** | 写 Go 代码 / 写 .md 技术文档 / 提到 PG/MySQL/Redis/Kafka | 注入 14 条铁律 + 命名规范 + 7 段技术方案模板 |
| 🐱 **orangecat** | 提测 / QA 交付 / 生成提测报告 | 自动生成 QA 版 + 开发版两份提测文档，含真实性校验 |
| 🧬 **dev-dna** | 任何 Go 任务 / 用户说"按我习惯" | 注入个人偏好（栈 / 命名风格 / 错误处理倾向）+ 反蒸馏声明 |
| 🔍 **code-review** | AI 写完代码 / 用户粘代码 | 自动按规范 review，P0/P1/P2/P3 分级 + 置信度过滤 + 修复方案 |

**5 个 Slash Command**（用户主动调用 `/<name>`）：

- `/tech-design` —— 按 7 段范式生成技术方案文档
- `/design-table` —— 按命名规范生成 CREATE TABLE
- `/api-doc` —— 按统一格式生成接口文档
- `/review` —— 按 14 铁律 + 置信度审查 git diff / 文件
- `/tixuebj` —— 主动触发 OrangeCat 生成提测文档

**13 条 Cursor Rules**（装到 `~/.cursor/rules/`）：

- 14 条铁律的 Cursor 版本（`alwaysApply: true`，每次对话强制注入）
- `.go` / `.sql` / `.proto` / `.md` / `.dockerfile` 文件的针对性规则
- 与 Claude Skill 共享单一源（`standards/*.md`），改一处两边同步

---

## 怎么用

### 1. 装

DMG 双击 → 拖到 Applications → 首次必跑：

```bash
xattr -cr /Applications/Team\ Standards.app
```

（macOS Gatekeeper 给未签名 App 加了隔离属性，ad-hoc 签名解决不了，必须手动清。）

### 2. 配

打开 App → 默认在「安装」tab → 选"全局" → 点"立即安装"。
覆盖 `~/.claude/skills/` 和 `~/.cursor/rules/`，自定义 `custom-*.md` 不会被清。

**必做**：装完 `Cmd+Q` 彻底退出 Claude Code / Cursor 再打开（Skill 只在启动时加载）。

### 3. 用

不用记任何命令。AI 会**自动**激活：

- 你写"帮我搭一个 Kratos service" → 🌟 go-team-standards 激活
- AI 写完代码 → 🔍 code-review 自动自检违规并修
- 你说"提测" → 🐱 orangecat 生成两份提测文档
- 你说"按我习惯写" → 🧬 dev-dna 注入个人偏好

回复末尾会打 emoji 标记（🌟🧬🐱🔍）让你**肉眼确认 skill 真生效**。没打 = 没触发。

### 4. 主动调

```
/review                       # review git diff
/review internal/wallet.go    # review 指定文件
/tech-design                  # 写技术方案
/design-table users           # 设计表
/tixuebj                      # 提测
```

---

## App 长啥样

**Apple Tahoe 浅色玻璃风**，1100×760 原生窗口，无浏览器无终端弹窗。

侧栏 7 个 Tab：

| Tab | 作用 |
|---|---|
| 📚 **规范模块** | 浏览所有 Skill，每个挂触发方式 pill（🔵 被动 / 🟢 主动 / 🟠 文件 / 🟣 始终）+ 关键字 |
| 🪪 **Skill 管理** | 添加团队规范外的个人/小组规则，自动同步到 Claude + Cursor + 分发包 |
| 🎭 **AI 人格** | 注入到每次对话的全局工作态度（严谨 / 不敷衍 / 勤勉 / 交付诚实） |
| 📋 **已装清单** | 看本机已装的 Skill / Rules / Commands，绿/橙/灰区分团队 / 自定义 / 第三方 |
| 🧪 **测试 Skill** | 用 20 条违规 prompt 测本地 LLM 能否稳定触发 + 识别 |
| 🪵 **日志控制台** | HTTP / Shell / API 调用日志 + 错误高亮 + 过滤 |
| ⚡ **安装** | 4 段：基础（Hero + 4 格统计 + 立即安装）/ 更新与同步 / 进阶 Skill / 打包分发 |

---

## 为什么不只是个 install.sh

### 1. 规范是活的

规范每周都在变（命名加新约定、铁律细化、Skill 加新触发）。App 内置「一键更新规范到最新」会**并发拉 5 个端点**（`/api/install` + 4 个独立 skill），自定义 `custom-*.md` 会保留。

### 2. 自定义规则一键同步

你团队特有的"JWT 必须带 X-Trace-ID"、"定时任务必 cron_ 开头"、"Redis key 必声明 TTL"——在 App 里加一条，自动同步到 Claude + Cursor + 下次分发的 zip 包，不用手动改 13 个文件。

### 3. 分发友好

App 一键生成给团队的安装包：
- **DMG**（推荐）—— macOS 原生镜像，挂载即用，签名不会被传输破坏
- **zip** —— 兼容更老的 macOS
- **Shell 脚本** —— 自解压 .sh，对方 `chmod +x && ./xxx.sh` 直接装 skill，**完全不需要这个 App**

### 4. Claude Desktop 单 Skill 导出

Claude Desktop（claude.ai 网页 / 桌面 App）支持单 Skill 导入。App 里点一个按钮：
- 导出 zip —— 完整带 references / templates
- 导出 SKILL.md —— 轻量纯 prompt

发给同事直接拖到 Claude Desktop 的 `Customize → Skills → +`。

### 5. 反蒸馏 + 隐私边界

**dev-dna Skill 头部含反蒸馏声明** + provider opt-out 指南：
- Anthropic Console → Privacy → 关 "Help improve Claude"
- Cursor → Privacy Mode 必开
- Copilot → 关 "Allow code snippet upload"
- 公司本地 Clawnova —— 数据不出公司

**禁止填**手机号 / 身份证 / 真名 / 公司机密项目代号。Profile 存 `~/Library/Application Support/TeamStandards/`，权限 0600 仅本人可读。

---

## 技术架构（一段）

Go 写的单 binary（12 MB），内嵌 webview_go（macOS WKWebView）。启动时背景起 HTTP loopback（127.0.0.1: 随机端口）提供 API，前端 UI（HTML/CSS/JS）通过 WKWebView 加载。**用户完全感知不到底下是 Web** —— 看起来就是个原生 macOS App。

所有规范文件、Skill 模板、Cursor Rules 通过 `//go:embed` 编译进二进制。装的时候从 embed 读出来写盘 `~/.claude/skills/...`。**不依赖网络，不依赖 Python / Node**。

---

## 当前版本

**v1.7.28** · 2026-05-06 · 见 [CHANGELOG.md](./CHANGELOG.md) 完整变更历史

里程碑：
- v1.7.21 项目级 Skill 同步
- v1.7.23 Slash Commands 系统
- v1.7.25 code-review 被动 Skill
- v1.7.26 Apple Tahoe UI 全面换肤
- v1.7.27 code-review v2.0（置信度 + P0-P3 分级 + 不报清单）
- v1.7.28 USAGE.md 配套使用文档

---

## 反馈 · 贡献

- App 内「日志控制台」看本机加载状况
- 规范本身有问题：反馈给团队规范 owner（IM / Issue）
- 想加 Skill / Rule：提 PR 改 `standards/*.md` 单一源，App 编译后自动同步两端

---

## 一句话送新成员

> 装完 App、Cmd+Q 重启 Claude / Cursor，下周开始，再没人在你 PR 里写 `float64 amount`。
> 🌟🧬🐱🔍
